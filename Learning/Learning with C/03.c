#include <stdio.h>  
 int main()  
 {  
    // int a, b, sum;
    // scanf("%d %d", &a, &b);
    // sum = a + b;
    // printf("Sum is: %d\n", sum);
    
    // double a, b, sum;
    // scanf("%lf %lf", &a, &b);
    // sum = a + b;
    // printf("Sum is: %0.2lf\n", sum);

    char ch;
    printf("Enter the first letter of your name: ");
    // scanf("%c", &ch);
    ch = getchar();
    printf("The fist letter of your name is: %c\n", ch);

    
    return 0;  

 }  